# Traceability

FR-001 → session/reset integration test. FR-002–004 → delegation UI/policy test. FR-005–008 → shared changes/audit tests. MCP-001–003 → compiler/inspector/lifecycle tests. SEC-001–004 → forged-input/no-apply-tool tests. CON-001 → deterministic conflict test.

Manual gates: M2 direct selection-without-delegation write fails; M3 displayed descriptors equal registrations; M4 narrowed mandate rejects stale invocation server-side; M5 rebase preserves target; M6 agent cannot apply and human apply is audited; final fresh reset replays full demo.
