# Deployment

Deploy one HTTPS origin with a small Worker + D1/SQLite-compatible store. Reset must restore deterministic seed data and clear mandates/changes. Verify human fallback when WebMCP is unavailable, Chrome/browser registration behavior, no cross-session leakage, and no exposed internal errors. Use deployment-platform authentication; commit no credentials. Record deployed URL, SHA, browser/version, eval inputs, limitations.
