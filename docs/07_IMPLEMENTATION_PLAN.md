# Implementation milestones

M0 bootstrap/session/reset/tests. M1 human CRM and activity log. M2 mandate UI/backend enforcement (gate: selected-but-not-delegated direct write fails). M3 WebMCP read + inspector (gate: inspector equals registrations). M4 bounded stage tools/shared edits (gate: stale authority fails server-side). M5 conflicts/rebase (gate: absolute intent/provenance survives). M6 human apply/audit (gate: no agent route/tool can apply). M7 evals/polish. M8 public deployment and fresh-session replay.
