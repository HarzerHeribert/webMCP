# Agent working agreement

Read relevant specs and tests first; name affected requirement IDs; implement vertically and minimally. Never silently override locked decisions or add an LLM, agent harness, API key, remote MCP server, browser extension, OAuth, accounts, or third-party integration. Before completion run typecheck, lint, unit/integration/browser tests as relevant and verify acceptance criteria. Recheck primary WebMCP guidance before API changes.
